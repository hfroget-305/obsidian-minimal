name: CRM lead intake

on:
  issues:
    types: [opened]
  workflow_dispatch:
    inputs:
      company:
        description: "Company (required)"
        required: true
      contact:
        description: "Contact name"
      email:
        description: "Email"
      phone:
        description: "Phone"
      source:
        description: "Source: website / referral / ads / event / outreach / other"
        default: "other"
      referrer:
        description: "Referrer (existing customer, for source: referral)"
      value:
        description: "Estimated value (USD, digits only)"
      next_action:
        description: "Next action"
      next_action_date:
        description: "Next action date (YYYY-MM-DD)"
      notes:
        description: "Notes"
  repository_dispatch:
    types: [new-lead]

permissions:
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: crm-lead-intake
  cancel-in-progress: false

jobs:
  create-lead:
    # For issue events, only act on issues created from the "New lead" form.
    # Match the form's title prefix as well as the label: GitHub only applies
    # template labels that already exist in the repo, so a fresh repo where
    # nobody has created the 'new-lead' label yet must not silently skip.
    if: >-
      github.event_name != 'issues' ||
      contains(github.event.issue.labels.*.name, 'new-lead') ||
      startsWith(github.event.issue.title, 'New lead:')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Create lead file from event payload
        id: lead
        run: python3 .github/scripts/lead_intake.py

      - name: Open pull request
        id: pr
        env:
          GH_TOKEN: ${{ github.token }}
          COMPANY: ${{ steps.lead.outputs.company }}
          LEAD_PATH: ${{ steps.lead.outputs.lead_path }}
          ISSUE_NUMBER: ${{ steps.lead.outputs.issue_number }}
          SLUG: ${{ steps.lead.outputs.slug }}
        run: |
          set -euo pipefail
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git"
          # Deterministic per-company branch: a second submission for the same
          # company fails on push while the first PR is still open, instead of
          # opening a duplicate PR for the same lead file.
          BRANCH="crm/lead-${SLUG}"
          git config user.name "crm-intake[bot]"
          git config user.email "crm-intake[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add "$LEAD_PATH"
          git commit -m "Add lead: $COMPANY (via ${{ github.event_name }})"
          git push -u origin "$BRANCH"
          BODY="Automated lead intake from \`${{ github.event_name }}\`.

          Record: \`$LEAD_PATH\` (status: \`new\`, first response due today)."
          if [ -n "$ISSUE_NUMBER" ]; then
            BODY="$BODY

          Closes #$ISSUE_NUMBER."
          fi
          PR_URL=$(gh pr create --title "New lead: $COMPANY" --body "$BODY" \
            --base "${{ github.event.repository.default_branch }}" --head "$BRANCH")
          echo "url=$PR_URL" >> "$GITHUB_OUTPUT"
          if [ -n "$ISSUE_NUMBER" ]; then
            gh issue comment "$ISSUE_NUMBER" --body "✅ Lead record created — review and merge $PR_URL"
          fi

      - name: Report failure on the intake issue
        if: failure() && github.event_name == 'issues'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue comment "${{ github.event.issue.number }}" --body \
            "❌ Lead intake failed — check the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for the reason. Most common: a lead file or an open lead PR for this company already exists, a malformed date, a non-numeric value, or an unknown source."
